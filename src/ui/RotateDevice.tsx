import { RotateIcon } from './Icons';

export function RotateDevice() {
  return (
    <aside className="rotate-device" role="status" aria-live="polite" aria-label="Rotate device">
      <RotateIcon />
      <h2>TURN TOWARD THE VEIL</h2>
      <p>Rotate your device to landscape for Mark&apos;s descent.</p>
    </aside>
  );
}
